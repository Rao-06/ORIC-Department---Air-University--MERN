# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Use Compass-style host URI and DB name separately
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=research_grant_db

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Frontend URL used in email links
FRONTEND_URL=http://localhost:3000

# Email Configuration (for password reset and verification)
# Option A: Gmail (recommended via App Password)
# - Enable 2-Step Verification on your Google account
# - Create an App Password (select "Mail" / "Other")
# - Set EMAIL_SERVICE=gmail and use the app password below
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password   # 16-char app password, not your login password
EMAIL_FROM=your_email@gmail.com      # must match EMAIL_USER

# Option B: Custom SMTP server (set these instead of EMAIL_SERVICE)
# EMAIL_HOST=smtp.yourserver.com
# EMAIL_PORT=587
# EMAIL_USER=your_smtp_user
# EMAIL_PASS=your_smtp_password

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Security
BCRYPT_ROUNDS=12
